{% set currentRoute = app.request.get('_route') %}
<nav class="mt-3">
	<ul class="nav nav-pills justify-content-end">
		<li class="nav-item">
			<a class="nav-link{{ currentRoute == 'accueil' ? ' active' : '' }}" aria-current="true" href="{{ path ('accueil') }}">Accueil</a>
		</li>
		<li class="nav-item">
			<a class="nav-link{{ currentRoute == 'portfolio' ? ' active' : '' }}" href="{{ path ('portfolio') }}">Portfolio</a>
		</li>
		<li class="nav-item">
			<a class="nav-link{{ currentRoute == 'contact' ? ' active' : '' }}" href="{{ path ('contact') }}">Contact</a>
		</li>		
	</ul>
</nav>